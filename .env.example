# Port Configuration
# These will be auto-assigned by `make init`
FRONTEND_PORT=3000
BACKEND_PORT=3001
ADMIN_PORT=3002
DYNAMODB_PORT=3003

# AWS Configuration (Development Only)
# In production, use IAM roles instead
AWS_ACCESS_KEY_ID=dummy
AWS_SECRET_ACCESS_KEY=dummy
AWS_REGION=us-east-1

# DynamoDB Configuration
TABLE_NAME=counter_table
DYNAMODB_ENDPOINT=http://dynamodb-local:8000

# CORS Configuration
# For development: localhost origins
# For production: your actual domain(s)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002

# Security Notes:
# 1. Never commit .env file to version control
# 2. Use different credentials for each environment
# 3. In production, use AWS Secrets Manager or Parameter Store
# 4. Rotate credentials regularly
